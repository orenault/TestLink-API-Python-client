language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"

  # command to install dependencies
install:
  - "pip install pytest"
  - "pip install . "

# command to run tests
script: pytest test/utest-offline
